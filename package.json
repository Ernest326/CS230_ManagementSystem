{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "lerna": "^8.2.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "dev": "lerna run dev",
    "test": "lerna run test"
  }
}
